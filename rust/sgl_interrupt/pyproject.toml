[build-system]
requires = ["maturin>=1.5,<2"]
build-backend = "maturin"

[project]
name = "sgl-interrupt"
requires-python = ">=3.9"
dynamic = ["version"]

[tool.maturin]
module-name = "sgl_interrupt"
features = ["pyo3/extension-module"]